// String Template
const name = "Yabsir Aditya";
const first = "Yabsir"
const last = "Aditya"

const result = `Nama : ${last} ${first}`;
const lama = "Nama : " + name;

console.log(result);
console.log(lama);

// string to number
const nilai = 80

const result2 = `Name : ${name}, Lulus : ${nilai > 70}`

console.log(result2);

// Multiline String